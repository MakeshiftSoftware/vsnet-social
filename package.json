{
  "name": "vsnet-social",
  "version": "1.0.0",
  "author": "taapesh",
  "private": true,
  "repository": "https://github.com/MakeshiftSoftware/vsnet-social",
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "dev": "nodemon --watch src ./src/server.js",
    "lint": "./node_modules/.bin/eslint src/**",
    "clean": "rm -rf node_modules && rm -f yarn.lock && yarn cache clean && yarn",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vsnet-logger": "MakeshiftSoftware/vsnet-logger#develop",
    "vsnet-protocol": "MakeshiftSoftware/vsnet-protocol#develop",
    "vsnet-socket": "MakeshiftSoftware/vsnet-socket#develop"
  },
  "devDependencies": {
    "eslint": "^4.18.2",
    "eslint-config-airbnb-base": "^12.0.0",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-node": "^5.1.1"
  }
}
